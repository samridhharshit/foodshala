const initState = {
    user:{
        access_token: null
    },
    restaurant: {
        access_token: null
    }
}

const rootReducer = (state = initState, action) => {
    return state
}

export default rootReducer